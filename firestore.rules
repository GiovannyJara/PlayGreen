rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permite leer y escribir en cualquier documento
    match /{document=**} {
      allow read, write: if true;
    }
  }
}